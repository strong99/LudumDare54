@page "/"

<div class="index menu view">
    <div class="panel">
        <h1>The Despatch Rider Of Hope</h1>

        <nav>
            @if (SessionManager.HasSession) {
                <a href="/play" @onclick:preventDefault @onclick="Continue"><span>Continue story</span></a>
            }
            <a href="/play" @onclick:preventDefault @onclick="StartNew"><span>New story</span></a>

            <a href="/settings"><span>Settings</span></a>

            @if (CanEditRepository) {
                <a href="/editor"><span>Editor</span></a>
            }

            @if (CanQuit) {
                <button type="button" @onclick="TryQuit"><span>Quit</span></button>
            }
        </nav>
    </div>
</div>
